# Legacy: ESLint + tsc covered by ci.yml. Kept disabled to avoid conflicting gates.
name: ESLint Check (legacy)

on:
  workflow_dispatch:

jobs:
  lint:
    if: false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Use ci.yml instead"
