# LEGACY — do not use for production.
# Canonical host is Vercel Git integration (see docs/PRODUCTION_OPS.md).
# Static export to GitHub Pages disables API routes and is not multiplayer-capable.
name: Deploy to GitHub Pages (legacy)

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Pages deploy disabled. Use Vercel."
