# LEGACY — do not use for production.
# Canonical host is Vercel. Firebase Hosting static export lacks Next API routes used by multiplayer.
name: Deploy to Firebase Hosting (legacy)

on:
  workflow_dispatch:

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Firebase Hosting deploy disabled. Use Vercel."
